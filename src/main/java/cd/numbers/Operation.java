package cd.numbers;

class Operation {
    Operator operator;
    int number;

    public Operation(Operator operator, int number) {
        this.operator = operator;
        this.number = number;
    }
}
